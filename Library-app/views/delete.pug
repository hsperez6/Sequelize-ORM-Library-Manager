extends layout

block content
  h1 Delete #{book.title}
  p
    | Are you sure you want to delete 
    em= book.title 
    | ?
  form(action=`/books/${book.id}/delete`, method="post")
    a(href=`/books/${book.id}/update`) No! Take me back! 
    | 
    input(type="submit", value = "Yes, I'm sure")
